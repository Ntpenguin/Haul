// Re-exports from @stripe/stripe-react-native for native platforms only
// Web imports stripe-native.web.ts instead (via Metro/Webpack platform resolution)
export { StripeProvider, useStripe } from '@stripe/stripe-react-native';
